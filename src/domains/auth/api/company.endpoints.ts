import { API_BASE_URL } from '../../../config/constants';

export const COMPANY_ENDPOINTS = {
  UPDATE: `${API_BASE_URL}/companies`,
  RESUBMIT_VERIFICATION: `${API_BASE_URL}/companies/verification/resubmit`,
}; 